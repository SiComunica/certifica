"use client"

import { useToast } from "@/components/ui/toast"

export function Toaster() {
  // Il componente Toaster non è più necessario perché 
  // abbiamo spostato tutta la logica nel ToastProvider
  return null
} 